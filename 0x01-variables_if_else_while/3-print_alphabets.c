#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
lower = "abcdefghijklmnopqrtsuvwxyz"
upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
putchar(lower);
putchar(upper);
return (0);
}