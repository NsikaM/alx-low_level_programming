#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	m = n%10
    if m<6 and m!=0
{
    printf("The last digit of",str[n],"is",str[m],"and is less than 6 and not 0")
}
    if m=0
{
    printf("The last digit of",str[n],"is",str[m],"and is 0");
}
    else
{
    printf("The last digit of",str[n],"is",str[m],"and is greater than 5")
}
	return (0);
}